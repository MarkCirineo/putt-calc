# PostgreSQL connection string
# Use Neon, Supabase, or local Postgres
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/putt_calc?schema=public"

# Auth.js (NextAuth) – generate secret: openssl rand -base64 32
AUTH_SECRET="generate-a-secret-key"
NEXTAUTH_URL="http://localhost:3000"
